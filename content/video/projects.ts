import type { Project } from "@/content/types";

/**
 * Schelet FAZA 0 — se umple în FAZA 2 (zona video).
 * Portofoliul e integral placeholder până la conținut real:
 * isPlaceholder: true, media locală cu poster obligatoriu.
 */
export const videoProjects: Project[] = [];
