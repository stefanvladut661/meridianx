import type { Service } from "@/content/types";

/**
 * Schelet FAZA 0 — se umple în FAZA 2 (zona video).
 * Toate intrările fără conținut real: isPlaceholder: true.
 */
export const videoServices: Service[] = [];
